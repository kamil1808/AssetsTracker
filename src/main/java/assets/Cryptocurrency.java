package assets;

public class Cryptocurrency extends Asset {
    Cryptocurrency(String category, String name, String code, double latestValue, double actualValue) {
        super(category, name, code, latestValue, actualValue);
    }
}
